console.log('Hello, I\'m your coding fun fact guide');

const botName = "Khalid's bot";
const botLocation = "The internet";
const favoritelangauge = "JavaScript";

let val;
val = `My name is ${botName} and I live on ${botLocation}`;
console.log(val);

var codingFact = (`In ${favoritelangauge} , typeof null returns "object" even thoguh null is not actually an object`);
console.log(codingFact);

var codingFact = (`${favoritelangauge} was originally created in just 10 days`);
console.log(codingFact);

var codingFact = (`Brendan Eich created the first version of ${favoritelangauge} in May 1995 while working at Netscape. It was initially called Mocha, then LiveScript, before becoming ${favoritelangauge}.
And the crazy part? The language we’re still using today came from that ridiculously short development period.`);
console.log(codingFact);

let farewellStatement = `It was fun sharing these facts with you. Goodbye! - ${botName} from ${botLocation} `;
console.log(farewellStatement);